# Get the length from the user
length = float(input("What is the length of the side?"))
print(length)
#calculate and store the area in a variable 
area = length * length
# Print the calculated area to the terminal
print(f"the calculated area of the square is {area}cm^2")